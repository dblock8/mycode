import pygam, death, sys
from sys.locals import *

heroes = (captainAmerica, ironMan, thor, hulk, drStange, blackPanther, blackWidow, vision, scarletWitch, spiderman, wong, falcon, warMachine, rocket, groot, drax, nebula, gamora, loki, heimdall)

stoneAcquired = []
gauntletFull = False
snap = 'notPossible'

places = ( asgardianShip, vormier, wakanda, titan, xandar, knowhere )

asgardianShip = 'asgradianShip'
vormier = 'vormier'
wakanda = 'wakanda'
titan = 'titan'
xandar = 'xandar'

war = True
while war:
    for event ininfinityWar.events.get():
       if event.type = SNAP:
           print("End is near")
           war = False

    for i in places:
       if places[i] == 'xandar':
           stoneAcquired.push('Powerne')

       if places[i] == 'asgardianShip':
           heroes.pop(loki)
           death.kill('Loki          heroes.pop(heimdall)
           death.kill("Heimdall         stoneAcquired.push('Spacene')

       if places[i] == 'knowhere'
           death.kill('Collector          stoneAcquired.push('Realityne')

       if places[i] == 'vormier':
           death.kill("Gamora         stoneAcquired.push('Soulne')

       if places[i] == 'titan':
           print("We are in the end game now")
           stoneAcquired.push('Timene')

       if places[i] == 'wakanda':
           heroes.pop(vision)
           death.kill('Vision          stoneAcquired.push('Mindne')
           gauntletFull = True
           snap = 'isPossible'
           if snap = 'isPossible':
               war = False

       infinityWar.update()

death.kill(universePopulation / 2)
print("Universe is balanced now")
infinityWar.quit()
Thanos.exit()
